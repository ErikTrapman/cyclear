{% extends 'admin.base_normal.twig' %}

{% block title %}Ploeg new{% endblock %}

{% block content %}
    <h3>Ploeg creation</h3>

    <form action="{{ path('admin_ploeg_create') }}" method="post">
        {{ form_widget(form) }}
        <p>
            <button class="btn btn-default" type="submit">Create</button>
        </p>
    </form>

    <ul class="record_actions">
        <li>
            <a href="{{ path('admin_ploeg') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}