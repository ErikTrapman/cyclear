{% extends 'admin.base_normal.twig' %}

{% block content -%}
    <h1>AwardedBadge</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Recurringamount</th>
                <td>{{ entity.recurringAmount }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_awardedbadge') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_awardedbadge_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
