{% extends 'admin.base_normal.twig' %}

{% block content -%}
    <h1>AwardedBadge edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_awardedbadge') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
