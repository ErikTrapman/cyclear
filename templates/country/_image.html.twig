{% if country %}<img style="margin-right:3px; margin-bottom:{% if size == 32 %}5px{% else %}2px{% endif %};"
                     width="{{ size }}" height="{{ size }}"
                     src="/icons/flags/{{ size }}/{{ country.iso2|lower }}.png"
                     alt="{{ country.name }}">{% endif %}
