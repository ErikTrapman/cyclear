<h1>Renner list TODO</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Naam</th>
            <th>Afkorting</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('admin_ploeg_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.naam }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_ploeg_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_ploeg_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('admin_ploeg_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
