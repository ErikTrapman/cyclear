{% extends '::admin.base.html.twig' %}

{% block content %}
<h3>Periode creation</h3>

<form action="{{ path('admin_seizoen_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_seizoen') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}