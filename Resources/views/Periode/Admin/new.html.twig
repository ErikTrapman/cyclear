{% extends '::admin.base.html.twig' %}

{% block content %}
<h1>Periode creation</h1>

<form action="{{ path('admin_periode_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_periode') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}