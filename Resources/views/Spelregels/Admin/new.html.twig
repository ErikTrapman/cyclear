{% extends '::admin.base.html.twig' %}


{% form_theme form _self %}

{% block _cyclear_gamebundle_spelregelstype_content_widget %}
 {{ form_widget(form, { 'attr': {'class': 'field span5'} } ) }}
{% endblock %}

{% block content %}
<h3>Spelregels creation</h3>

<form action="{{ path('admin_spelregels_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_spelregels') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}