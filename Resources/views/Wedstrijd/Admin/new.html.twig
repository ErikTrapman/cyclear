{% extends '::admin.base.html.twig' %}

{% block content %}
<h3>Wedstrijd creation</h3>

<form action="{{ path('admin_wedstrijd_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_wedstrijd') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}