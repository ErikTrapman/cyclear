{% extends '::admin.base.html.twig' %}

{% block content %}
<h2>Contract edit</h2>

<form action="{{ path('admin_contract_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_contract') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}