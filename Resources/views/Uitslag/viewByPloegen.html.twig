{% block content %}
<table class="records_list table table-condensed ">
    <thead>
        <tr>
            <th>Naam</th>
            <th>Punten</th>
        </tr>
    </thead>
    <tbody>
    {% for ploeg in list %}
        <tr>
            <td><a href="{{ path('ploeg_show', { "seizoen": seizoen.slug, id:ploeg.id}) }}">{{ ploeg.naam }}</a></td>
            <td>{{ ploeg.punten }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}