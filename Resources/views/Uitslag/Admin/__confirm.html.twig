{% extends '::admin.base.html.twig' %}

{% block content %}
<h1>Uitslag confirm</h1>

{% form_theme _self %}

<form action="{{ path('admin_uitslag_post') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_uitslag') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}