{% extends '::admin.base.html.twig' %}

{% block title %}Ploeg new{% endblock %}

{% block content %}
<h2>Ploeg creation</h2>

<form action="{{ path('admin_ploeg_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_ploeg') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}