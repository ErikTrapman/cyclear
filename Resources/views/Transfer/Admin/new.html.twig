{% extends '::admin.base.html.twig' %}

{% block title %}Transfer new{% endblock %}

{% block content %}
<h2>Transfer creation</h2>

<form action="{{ path('admin_transfer_new') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_transfer') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}