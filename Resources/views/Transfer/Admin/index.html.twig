{% extends '::admin.base.html.twig' %}

{% block title %}Transfer index{% endblock %}

{% block content %}
<h1>Transfer list</h1>
<ul>
    <li>
        <a href="{{ path('admin_transfer_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
<table class="records_list table ">
    <thead>
        <tr>
            <th>Id</th>
            <th>Renner</th>
            <th>Ploegvan</th>
            <th>Ploegnaar</th>
            <th>Datum</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('admin_transfer_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.renner }}</td>
            <td>{{ entity.ploegVan }}</td>
            <td>{{ entity.ploegNaar }}</td>
            <td>{{ entity.datum|date('Y-m-d H:i:s') }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_transfer_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_transfer_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
<div id="navigation">
    {{ entities.render()|raw }}
</div>
<ul>
    <li>
        <a href="{{ path('admin_transfer_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock %}