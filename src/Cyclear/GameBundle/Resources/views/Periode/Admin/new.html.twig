{% extends '::admin.base_normal.twig' %}

{% block content %}
    <h3>Periode creation</h3>

    <form action="{{ path('admin_periode_create') }}" method="post">
        {{ form_widget(form) }}
        <p>
            <button class="btn btn-default" type="submit">Create</button>
        </p>
    </form>

    <ul class="record_actions">
        <li>
            <a href="{{ path('admin_periode') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}